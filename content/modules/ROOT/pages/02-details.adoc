= Workshop details
:toc:
:toc-placement: preamble
:icons: font

include::partial$_attributes.adoc[]

== Timing and schedule

=== Full workshop (2 - 2.5 hours)

* **Module 1**: Tekton fundamentals (30 minutes)
* **Module 2**: Creating Tasks and TaskRuns (35 minutes)
* **Module 3**: Building your first pipeline (35 minutes)
* **Module 4**: Pipeline as code (30 minutes)
* **Module 5**: Monitoring and troubleshooting (20 minutes)

=== Abbreviated workshop (1 hour)

* **Module 1**: Tekton fundamentals (30 minutes)
* **Module 2**: Creating Tasks and TaskRuns (30 minutes, abbreviated)

== Technical requirements

=== Software versions

* Red Hat OpenShift {ocp_version}
* Red Hat OpenShift Pipelines {pipelines_version}
* OpenShift CLI (`oc`) version 4.20
* Tekton CLI (`tkn`) version 0.38 or later
* Web browser (Chrome, Firefox, Safari, or Edge)

=== Environment access

Participants need access to:

* OpenShift {ocp_version} multinode cluster (provided in the lab)
* Gitea Git server (provided in the lab)
* Web terminal accessible from the OpenShift console

=== Network requirements

* Internet connectivity is required for this lab, the OpenShift environment used is cloud hosted
* All required images and resources are pre-deployed in your lab environment

== Environment details and setup

=== Workshop links and details

The connection details for the environment you will use are as below, come back here if you need to revisit these details:

The guide should provide the needed links timely, but if you need them you have them here too.

* OpenShift console is accessible at:
** {openshift_console_url}[window=_blank]
* You can log in with your user:
** Username: `{OPENSHIFT_USERNAME}`
** Password: `{OPENSHIFT_PASSWORD}`
* Gitea is accessible at:
**  {gitea_console_url}[window=_blank]
** Username: `{OPENSHIFT_USERNAME}`
** Password: `{OPENSHIFT_PASSWORD}`

* The web terminal is accessible and running:
** {openshift_console_url}terminal[window=_blank]

=== Pre Workshop setup

Before starting we need to log into the OpenShift web console and prepare it for the workshop.

NOTE: OpenShift ships with a web-based console that will allow users to perform various tasks via a browser.

==== Log in to the OpenShift Web Console

`Click on the 'OpenShift Console' button below`

[link={openshift_console_url}]
[window=_blank, align="center"]
[role='params-link']
image::developer-console-button.png[Developer Workspace - Button, 300]

. You can log in with your user if needed:
.. Username: `{OPENSHIFT_USERNAME}`
.. Password: `{OPENSHIFT_PASSWORD}`

`Open the 'OpenShift Web Terminal' by clicking the button below`
[link={openshift_console_url}/terminal]
[window=_blank, align="center"]
[role='params-link']
image::developer-webterminal-button.png[Developer Workspace - Button, 300]

. Log in with your user if needed:
.. Username: `{OPENSHIFT_USERNAME}`
.. Password: `{OPENSHIFT_PASSWORD}`

. When logged in you might see this pop-up. Click on **Skip Tour**
+
image::ocp-skip-tour.png[OpenShift Tour Pop-up,link=self,window=blank,width=700]
+
. If it's the first tine you start the Terminal you will need to initialize it. In the *Initialize Terminal* view enter the following:
.. Project: Create Project
.. Project Name: `tekton-workshop-%OPENSHIFT_USERNAME%`
+
image::terminal-create-project.png[Initialize terminal,link=self,window=blank,width=700]
+
. Click on start and wait for the terminal to load.

. Keep the Terminal tab/window with the active terminal open during the whole workshop. You will work extensively in the terminal during the excersises.
+
image::terminal-running.png[OpenShift Terminal running,link=self,window=blank,width=700]

[NOTE]
====
If you, anytime during the workshop, are logged out - run the below command to log in again

[source,sh,role="copypaste"]
----
oc login https://api.%APPS_HOSTNAME_SUFFIX%:6443 -u %OPENSHIFT_USERNAME% -p %OPENSHIFT_PASSWORD%
----

If the terminal window stops responding overall(the terminal runs as a pod in your project) try to open a new instance using the link provided above

Or open it from the terminal icon in the top right corner or the OpenShift console as shown below, and pop it out to a separate window.

image::terminal-open-icon.png[OpenShift open terminal ,link=self,window=blank,width=400]

If the command does not work for CLI login, ask your facilitator for help.

====

////
Run the below command in the terminal to create the OpenShift project(namespace) you will use throughout this workshop:

[source,sh,role="copypaste"]
----
oc new-project tekton-workshop-%OPENSHIFT_USERNAME%
----

Expected output:

----
Now using project "tekton-workshop-%OPENSHIFT_USERNAME%" on server "...".
----
////

=== Setup validation

Run these commands in the **Terminal** tab to verify your environment is ready:

==== Verify OpenShift CLI version
[source,sh,role="copypaste"]
----
oc version --client
----

==== Verify Tekton CLI version

[source,sh,role="copypaste"]
----
tkn version
----

Great, let's dive into the excersises on the next page in *Module 1: Tekton fundamentals*